# ============================================================
# FinNexus — Data Pipeline Requirements
# Python 3.11.6
# ============================================================

# ── Core ────────────────────────────────────────────────────
pandas>=2.0.0,<2.3.0
numpy>=1.24.0,<2.3.0
requests>=2.31.0
python-dotenv>=1.0.0
tqdm>=4.66.0

# ── Visualisation & Math ────────────────────────────────────
matplotlib>=3.7.0,<4.0.0
scipy>=1.11.0

# ── ML / Boosting ───────────────────────────────────────────
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# ── India Equities ──────────────────────────────────────────
# NOTE: nselib had a major version bump (0.2 → 2.x); pin to latest
#       only if you've updated your code to match the new API.
#       Otherwise lock to nselib==0.2.0 for backward compatibility.
nselib>=2.5.0
nsepy==0.8
yfinance>=0.2.28,<1.0.0
pyzdata>=1.0.0

# ── India F&O ───────────────────────────────────────────────
jugaad-data>=0.3.0
nsefin==0.1.5

# ── US ETFs ─────────────────────────────────────────────────
# alpha-vantage 3.x has breaking API changes vs 2.x — keep 2.3.1
# if your code uses the v2 client interface.
alpha-vantage==2.3.1
pandas-datareader>=0.10.0

# ── Commodities ─────────────────────────────────────────────
oilpriceapi>=1.0.0
eodhd>=1.0.0
eia-ng-client>=0.1.80
beautifulsoup4>=4.12.0
lxml>=4.9.0

# ── Crypto ──────────────────────────────────────────────────
ccxt>=4.1.0

# ── File I/O ────────────────────────────────────────────────
openpyxl>=3.1.0
xlrd>=2.0.0

# ── Bot / Backend / RAG ─────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
openai>=1.0.0
chromadb>=0.4.22
sentence-transformers>=2.2.0
httpx>=0.24.0

# ── Authentication & Security ────────────────────────────────────────────────
PyJWT>=2.8.0
slowapi>=0.1.9

# ── Database (Supabase production) ───────────────────────────────────────────
supabase>=2.3.0

# ── Session store (Redis optional — SQLite used if not configured) ───────────
redis>=5.0.0

# ── Optional LLM provider SDKs ───────────────────────────────────────────────
# anthropic>=0.25.0
# cohere>=5.0.0

# ── Optional Async Utilities ─────────────────────────────────────────────────
# aiohttp>=3.8.0
# orjson>=3.8.0